{
  "name": "bentools/etl",
  "description": "Some classes to implement the ETL pattern, with the ability to hook on each event.",
  "minimum-stability": "stable",
  "license": "MIT",
  "authors": [
    {
      "name": "Beno!t POLASZEK"
    }
  ],
  "require": {
    "php": ">=7.1",
    "psr/log": "^1.0"
  },
  "autoload": {
    "psr-4": {
      "BenTools\\ETL\\": "src"
    }
  },
  "autoload-dev": {
    "psr-4": {
      "BenTools\\ETL\\Tests\\": "tests"
    }
  },
  "require-dev": {
    "phpunit/phpunit": "^6.0",
    "symfony/event-dispatcher": "^3.2",
    "doctrine/orm": "^2.5",
    "guzzlehttp/promises": "^1.3"
  },
  "suggest": {
    "symfony/event-dispatcher": "An event dispatcher to hook into the ETL process.",
    "guzzlehttp/promises": "Process Asynchronous ETL",
    "cocur/slugify": "Generates slugs from strings - useful to create array keys."
  }
}
